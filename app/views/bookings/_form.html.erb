<%= simple_form_for [@vehicule, @booking] do |f| %>
<%= f.input :check_in, { class: 'input-daterange' } %>
<%= f.input :check_out, { class: 'input-daterange' } %>
<%= f.submit 'Confirm booking', { class: 'button-yellow'} %>
<% end %>
